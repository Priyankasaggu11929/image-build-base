rebuild_branch_test-obs-build:
  steps:
    - trigger_services:
        project: home:psaggu:test
        package: image-build-base
    - rebuild_package:
        project: home:psaggu:test
        package: image-build-base
  filters:
    event: push
    branches:
      only:
      - test-obs-build
